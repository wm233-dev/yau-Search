# 交付成果使用指南（HOW_TO_USE）

> 全部产物在 `E:\deepseek_exclusive\math\.tmp\burn2026\`，没有动仓库里任何既有文件。
> 先读 `FINAL_REPORT.md`（总报告），再按下面的场景挑入口。

---

## 0. 三分钟上手

| 你想干什么 | 打开哪个文件 | 说明 |
|---|---|---|
| **一眼看全貌** | `FINAL_REPORT.md` | 237 行总报告：语料规模、12 条核心发现、上游纠错、口径与缺陷 |
| **翻题库（推荐）** | `yau_index.html` | 双击用浏览器打开。按 **年份/科目/卷别/考点标签** 下拉筛选 + 关键词全文搜索，757 题实时过滤 |
| **看纯文本索引** | `reports/all_problems_index.md` | 220 KB，按年份-科目-题号排序的全部 757 题（题面前 240 字） |
| **当数据用** | `data/problems_full.json` | 唯一权威口径：757 条，字段 year / subject / paper / kind / n / chars / text |

`yau_index.html` 是自包含单文件（510 KB，题库内嵌），**不依赖网络、不需要服务器**，拷到哪台机器都能开。

---

## 1. 场景 A：我要开始备考（最常用）

按这个顺序走，不要跳：

**第 1 步 —— 先确定"考什么、不考什么"**
打开 `reports/syllabus_coverage.md`。6 份官方考纲拆成 319 条，逐条标了真题命中情况：
- **A 级（18 条）**：命中密集且近年上升 → 必须拿下
- **153 条零命中（48%）**：考纲写了但 17 年从没考过 → 文件 `4.8 给了"可战略性放弃"清单
- 注意 `5 的**超纲反向发现**：代数卷里 25% 是线性代数、几何卷里 33% 是点集拓扑——考纲没写但年年考

**第 2 步 —— 定 12 周计划**
`reports/study_roadmap.md`（1418 行）。每个 TOP 考点都给了「**书名+作者+出版社 → 具体章节号 → 配套真题年份题号**」三段式，书全部来自你机器上真实存在的藏书（969 本有可用目录）。
- `7 还复核了 `LIBRARY_GAPS.md`，发现 1 条假阴性（Kolmogorov–Fomin 其实在库，BK0940），并新增 3 条 A 级缺口：**统计物理、通用数值分析、当代代数拓扑**
- `5 给了"战略性放弃 6 项"及其影响面（最多 2–3 题）

**第 3 步 —— 摸底**
做 `reports/mock_exam_A.md` / `mock_exam_B.md` / `mock_exam_C.md`：
- A = 代数+分析，B = 几何+概率，C = 计算+数学物理，各 6 题/100 分/150 分钟
- `1 是可直接打印的题面，`2 是参考答案 + 满分/部分分/零分判据，`4 是失分预警
- 三份都注明了"因为给不出可靠答案而换掉的题"（共 25 道）——**换题记录本身也是考点情报**

**第 4 步 —— 按母题刷，不要按年份刷**
`reports/mother_*.md` 是本次最有价值的成果（见 `4 详述）。

---

## 2. 场景 B：我要押题 / 决定放弃什么

| 问题 | 去哪儿看 |
|---|---|
| 哪些考点年年考？ | `reports/mother_*.md` 的 `1 母题总表（含"是否仍在考"列） |
| 哪些考点已经死了？ | 同上，**停考母题**一栏。几何的经典微分几何计算类 9 个母题末现全 ≤2018；计算的图论块 2018 后消失、算法数论块 2017 后消失；概率的贝叶斯 2018 止、重随机化 2021 止 |
| 命题人怎么"换皮重考"？ | 各母题小节里的**升级链**。例：正调和函数考了 10 年——常数 → a+b\|x\|^(2−n) → 去正性改增长条件 → L² 版 → \|x\|u(x) 极限存在 |
| 哪些方向 17 年零命中？ | `reports/syllabus_coverage.md` `5 + 各科目报告 `8（代数几何、辛几何、示性类、弦论、超对称、量子信息、信息论、鞅论正面考点……） |
| 难度什么时候变的？ | `reports/stats_overview.md` `3。**分水岭是 2023 年**：平均小问数 0.53→2.69、题面词数 73→107 |

---

## 3. 场景 C：我要看某道题怎么解

| 材料 | 覆盖范围 | 可信度 |
|---|---|---|
| **官方解答** | 只有 **2020 / 2021 / 2022** 三年（初赛 16 份） | 最高。原文在 `txt/` 里搜 `soln` |
| `reports/solutions_algebra.md` 等三份讲义 | 每科 10 道精讲（共 30 道），含完整分步解答 | **已过对抗性审稿**，见下 |
| `reports/referee_*.md` | 三份审稿报告 | **必读**：讲义里哪些地方不能照背 |
| 各年代/科目报告 | 代表题 3–8 道的"解题思路"（不是完整解答） | 中等 |

**审稿结论摘要**：30 道题的**最终答案全对、主证明全部通过**，但共 10 处"顺口说的补充/小结"是错的。审稿报告把每处都给了修正文本，且已回填进讲义（修正位置带 `✅ 已按 referee_*.md 修正` 标记）。

---

## 4. 场景 D：母题表怎么用（本次最大的用法变化）

> 结论先行：**这套比赛的题几乎不会原样重复，但会不断"换皮 + 升要求"地重考同一个数学内核。**
> 自动查重在这套题库上漏检率 >85%（实证：几何 11T5 与 24I4 是同一道题，跨 13 年，字符相似度只有 **0.013**）。

所以正确的刷题单位是**母题**，不是年份卷：

1. 打开 `reports/mother_<科目>.md` 的 `1 总表，按"性价比"排序（= 出现次数 × 近年权重）
2. 每个母题读 `2：数学内核 → 各年版本的差异 → **升级链** → 标准解法骨架（3–8 步）→ 最容易卡住的一步
3. 把该母题的所有成员题（年份/卷别/题号）从 `yau_index.html` 里筛出来，**连着做**——体会"同一内核怎样被加码"
4. 机器可读版在 `data/mother_*.json`，可以自己写脚本做二次统计

**覆盖情况**：757 题中 **675 题（89.2%）** 被归入母题，82 道是孤题（其中一批集中在 2022–2026，是新母题种子，值得重点关注）。

⚠️ 六个 JSON 的 schema 不统一（概率把孤题也放进了 `mothers`，计算单独给了 `orphans`），**母题总数不要直接相加**，按各科目报告自述数字使用。我已用 `scripts/validate_mothers2.py` 校验：成员引用 **0 处编造**。

---

## 5. 场景 E：当数据用 / 二次开发

```python
# 读题库（纯标准库，不需要装任何东西）
import json, io
P = json.load(io.open(r"E:\deepseek_exclusive\math\.tmp\burn2026\data\problems_full.json", encoding="utf-8"))
print(len(P))                      # 757
ana = [p for p in P if p["subject"] == "Analysis & PDE"]   # 155 题
```

| 文件 | 内容 |
|---|---|
| `data/problems_full.json` | 757 题完整题面（权威口径） |
| `data/problems_enriched.json` | 上述 + 考点标签 / 词数 / 小问数 / 难度代理 / 动词分布 |
| `data/mother_*.json` | 六科母题与成员 |
| `data/syllabus_coverage.json` | 319 条考纲的逐条命中明细 |
| `data/finals_index.json` `data/finals_manifest.json` | 总决赛 195 份卷的映射与元数据 |
| `data/papers_manifest.json` | 136 份初赛卷的解析元数据 |
| `data/near_duplicates.json` `data/text_families.json` | 近重复题对（**注意漏检率 >85%**，别单独依赖） |

**想重建全部报告**：`scripts/rebuild_pipeline.py` 是唯一权威流水线（抽取 → 合并碎片 → 打标签 → 出统计 → 出 HTML）。新增真题后只要把 PDF 丢进 `txt/` 再跑一次即可。

**想读 PDF / 渲染页面**（PyMuPDF 是本地 vendored 的，必须先设路径）：
```powershell
$env:PYTHONPATH='E:\deepseek_exclusive\math\pylibs'
python your_script.py
```

---

## 6. 几个必须知道的口径坑

1. **难度有两套，不能相减**：`problem_metrics` 里的是启发式"难度代理"（长度+小问数+符号密度）；各科目报告里的"难度 1–5"是人工自评。
2. **题面是 PDF 文本层抽取，不是 OCR**：公式会丢负号/上下标/根号（例如 2019 分析个人 Q5 抽成 Δu=…，原题几乎必然是 −Δu=…）。可疑处各报告都标了"抽取存疑"。
3. **F 盘的笔试真题只到 2025，没有 2026**；2026 题面只能依赖抽取文本，已知至少 3 处符号可疑。
4. **题库在 12:16–18:45 之间被修订过**（757→751→758→757）：各报告若自述题量与 757 不符，以报告自己声明的口径为准，别硬对齐。
5. **关键词命中是下界**：考纲"零命中"应读作"从不以该术语形态出现"，不等于绝对不考。

---

## 7. 还没做的事（你接手时从这里开始）

1. **讲义修正已回填，但讲义的"存疑清单"里仍有未裁定项** —— 各 `solutions_*.md` `12 节列着"放弃的题"和"引用标准定理未重证"的地方。
2. **总决赛语料还没做统一题库化**：三份总决赛报告各自切题（244 / 241 / 117 题），口径不同，建议用一个脚本并入 `problems_full.json` 的结构。
3. **2026 总决赛卷 + 官方考纲更新**：F 盘总决赛目录里有 `.doc/.docx/.JPG` 格式的原卷（已被发现并抢救回 11 份），下次抓取别忘了非 PDF 格式。
4. **藏书缺口**：统计物理、通用数值分析、当代代数拓扑三类教材本机确实没有，而统计物理是 2022–2026 的连年考点。
