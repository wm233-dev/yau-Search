
=== page 1 ===
Algebra and Number Theory
Individual Oral Test
1. Consider f ∈Z>0 and nonzero vector spaces Vi indexed by i ∈Z/fZ.
Suppose that there are linear maps φi : Vi →Vi+1 and ψi : Vi →Vi−1 such
that
φi−1 ◦ψi = 0,
ψi+1 ◦φi = 0.
(We may think of a circular graph with oriented edges such that the “Orpheus
condition” holds: Whenever you turn back while traveling through the graph
you are killed.)
Prove that there exists lines ℓi ⊂Vi for every i ∈Z/fZ such that
φi(ℓi) ⊂ℓi+1,
ψi(ℓi) ⊂ℓi−1
under one of the following two conditions:
1. all ψi = 0, or
2. dim Vi are equal to each other.
Hint: use induction
2. For k non-negative integer, let Vk := R[x]≤k be the vector space of real
polynomials of degree at most k with an action by SL2(R) by
γ · P(x) = (cx + d)kP
ax + b
cx + d

,
γ =
a
b
c
d

∈SL2(R).
1. Show that Vk is an irreducible representation of SL2(R);
1

=== page 2 ===
2. For non-negative integers m, n, consider Vm,n := Vm ⊗Vn as a subspace
of C[x, y] of polynomials with both x, y-degrees at most k. Assume
m ≥n ≥1. Show that following exact sequence is exact and split as
representations of SL2(R).
0−→Vm−1,n−1
·(y−x)
−→Vm,n
y=x
−→Vm+n−→0.
This implies the following decomposition of representations:
Vm ⊗Vn = ⊕n
i=0Vm+n−2i.
3. For non-negative integers ℓ≥m ≥n consider the space of invariants
(Vℓ⊗Vm ⊗Vn)SL2(R).
Show that this space is either trivial or one-
dimensional; it is non-trivial if and only if
ℓ+ m + n ≡0
mod 2,
ℓ+ m ≥n.
3. Is (x2 + 3)(x3 + 2) solvable mod p for every prime p?
2
